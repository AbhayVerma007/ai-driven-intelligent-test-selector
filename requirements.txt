scikit-learn>=1.3.0
pandas>=2.1.0
numpy>=1.24.0
joblib>=1.3.0
pytest>=7.4.0
gitpython>=3.1.0
pyyaml>=6.0
matplotlib>=3.8.0

# Optional / later phases — not required for Phase 1 (collector/trainer/selector)
# transformers>=4.35.0        # semantic_analyzer.py (CodeBERT) — only if model accuracy < 85%
# torch>=2.1.0                # semantic_analyzer.py (CodeBERT) — only if model accuracy < 85%
# seaborn>=0.13.0             # cost_tracker.py visualizations
# mlflow>=2.8.0                # model versioning / experiment tracking
# fastapi>=0.104.0            # optional dashboard API
# uvicorn>=0.24.0              # optional dashboard API
# xgboost>=2.0.0               # alternative to sklearn GBC — not used, keeping deps minimal
